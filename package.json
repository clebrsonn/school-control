{
  "name": "school-control",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm --prefix backend run dev\" \"pnpm --prefix front-end run dev\"",
    "build": "concurrently \"pnpm --prefix backend run dev\" \"pnpm --prefix front-end run build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "concurrently": "^9.1.2"
  }
}
